export enum AppStage {
  INTRO = 'INTRO',
  HEART_INTERACTION = 'HEART_INTERACTION',
  MESSAGE_SEQUENCE = 'MESSAGE_SEQUENCE',
  FINALE = 'FINALE',
}

export interface Position {
  x: number;
  y: number;
}